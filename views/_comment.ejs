<li>
	<p>
		<% if((locals.user) && (locals.user.id == comment.user.id)){ %>
		<small>
			<a href="/comments/delete/<%= comment.id %>">DELETE</a>
		</small>
		<% } %> <%= comment.content %>
		<br />
		<%= comment.user.name %>
	</p>
</li>
